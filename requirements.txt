# Production deps for deploying to Render. Mirrors what the local venv has.
# Two layers:
#   1. The openreversefeed library (installed in editable mode at the bottom)
#      so its dependencies are picked up via pyproject.toml
#   2. Django + production-server bits the Django reference app needs

# ─── Django + production server ───
Django>=5.1,<7
gunicorn>=21.2,<23
whitenoise>=6.6,<7
requests>=2.31,<3

# ─── Library deps (kept in sync with pyproject.toml) ───
# pinned conservatively for reproducible deploys
pandas>=2.1,<3
pyarrow>=15
openpyxl>=3.1,<4
xlrd>=2.0,<3
dbfread>=2.0,<3
SQLAlchemy>=2.0,<2.1
alembic>=1.13,<2
psycopg[binary]>=3.1,<4
pydantic>=2.6,<3
pydantic-settings>=2.2,<3
httpx>=0.27,<1

# ─── Install the library itself (editable) so `from openreversefeed...` works ───
-e .
